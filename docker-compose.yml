services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8080:8080"
    env_file:
      - .env

  web:
    build:
      context: ./apps/web
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://localhost:8080
    depends_on:
      - api
